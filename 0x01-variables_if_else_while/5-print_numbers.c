#include <stdio.h>
/**
 * main - prints all single digit numbers of base ten startung from zero
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0, n < 10; n++)
	{
		pritf("%d", n);
	}
	printf('\n');
	return (0);
}
